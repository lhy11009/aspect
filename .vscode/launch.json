{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            // Resolved by CMake Tools:
            "program": "${workspaceFolder}/build_equal-t-function/aspect",
	    // *** replace --test by the name of the .prm file you want to run: ***
            "args": ["/mnt/lochy/ASPECT_DATA/EntropySubduction/test_equal-t-function/entropy_adiabat/entropy_adiabat.prm"],
            "stopAtEntry": true,
	    // *** append to the path here if you want to run inside some directory like /cookbooks/ ***
            "cwd": "${workspaceFolder}"
        }
    ]
}
