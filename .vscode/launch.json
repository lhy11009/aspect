{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            // Resolved by CMake Tools:
            "program": "${workspaceFolder}/build_main_collision/aspect-debug",
	    // *** replace --test by the name of the .prm file you want to run: ***
            "args": ["case_debug.prm"],
            "stopAtEntry": true,
	    // *** append to the path here if you want to run inside some directory like /cookbooks/ ***
            "cwd": "/mnt/lochy/ASPECT_DATA/Collision0/collision_setup1/D1000_minV2.5e+19_WLV2.5e+19_PC5.0e-02_CTboth",
            "environment": [
                {
                    // add the directory where our target was built to the PATHs
                    // it gets resolved by CMake Tools:
                    "name": "config",
                    "value": "Debug"
                }
            ]
        }
    ]
}
