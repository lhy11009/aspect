{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            // Resolved by CMake Tools:
            "program": "${workspaceFolder}/build/aspect-debug",
	    // *** replace --test by the name of the .prm file you want to run: ***
            "args": ["isostatic_topography_box_2d_particles_small.prm"],
            "stopAtEntry": true,
	    // *** append to the path here if you want to run inside some directory like /cookbooks/ ***
            "cwd": "/mnt/lochy/ASPECT_DATA/Collision0/collision_setup_test/test_isostatic_topography_box_3",
            "environment": [{"name": "config", "value": "Debug"}],
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}
